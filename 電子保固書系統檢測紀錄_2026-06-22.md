# 電子保固書系統檢測紀錄

- 首次檢測時間：2026-06-22 10:58:53
- 最後更新時間：2026-06-22 11:11:38
- 檢測對象：GitHub Pages 公開站 + Apps Script `@34` admin deployment
- 檢測方式：live HTTP 檢查、實際頁面導覽、主要 API 回應驗證、前端 console 檢查、入口修復後再驗證
- 結論：**主要公開入口已整理完成，核心讀取/查詢路線正常；寫入型流程仍未做正式 smoke test**

## 一、本次檢測範圍

### 公開站
- `index.html`
- `repair.html`
- `admin.html`
- `query.html`
- `portal.html`
- `api-config.js`
- 字型資源 `assets/fonts/LINESeedTW_Rg.woff2`

### Apps Script API（live）
- `listWarrantyRecords`
- `listRepairs`
- `getWarrantyByToken`
- `exec?page=dashboard`（舊入口確認用）

## 二、檢測結果摘要

### 通過
1. `admin.html` live 可開啟（HTTP 200）
2. `query.html` live 可開啟（HTTP 200）
3. `index.html` / `repair.html` / `api-config.js` / 字型資源皆可存取（HTTP 200）
4. `admin.html` → `query.html` 互跳已修正，建立頁的「查詢保固書」按鈕可正常進入查詢頁
5. `query.html` 可成功載入案件清單，實測顯示：`已載入 1 筆案件｜待處理報修 0 筆`
6. `query.html` 返回 `admin.html` 正常
7. `listWarrantyRecords` API 正常
8. `listRepairs` API 正常
9. `getWarrantyByToken` API 正常
10. 後台建立頁 / 查詢頁載入時未觀察到 console error
11. `portal.html` 已改為目前可用的公開後台入口
12. `portal.html` live 實測可進入 `admin.html`

### 已修復項目
1. `portal.html` 舊的 `?page=dashboard` 入口已移除
   - 修復 commit：`6c5ad74`
   - commit message：`Update portal to use public admin/query entry pages`
   - 現在 live 顯示：
     - `建立保固書`
     - `查詢保固書`
     - `切換 Google 帳號`
   - 不再顯示：
     - `使用 Google 登入`
     - 舊的 `dashboard` 連結

### 目前仍需注意
1. `exec?page=dashboard` 舊 Apps Script 路徑本身仍會回：
   - `{"success":false,"message":"Unknown action"}`
   - 但因為公開入口 `portal.html` 已不再使用它，所以目前不影響公開站入口流程

## 三、實測細節

### 1. Live HTTP 狀態
- `https://harry0715-glitc.github.io/electronic-warranty-book/` → 200
- `https://harry0715-glitc.github.io/electronic-warranty-book/index.html` → 200
- `https://harry0715-glitc.github.io/electronic-warranty-book/repair.html` → 200
- `https://harry0715-glitc.github.io/electronic-warranty-book/admin.html` → 200
- `https://harry0715-glitc.github.io/electronic-warranty-book/query.html` → 200
- `https://harry0715-glitc.github.io/electronic-warranty-book/portal.html` → 200
- `https://harry0715-glitc.github.io/electronic-warranty-book/api-config.js` → 200
- `https://harry0715-glitc.github.io/electronic-warranty-book/assets/fonts/LINESeedTW_Rg.woff2` → 200

### 2. Apps Script API 檢查
- `listWarrantyRecords`：成功，案件數 `1`
- `listRepairs`：成功，報修數 `0`
- `getWarrantyByToken`：成功，可取得案件 `FG-2026-003`
- `exec?page=dashboard`：失敗，回 `Unknown action`（但已不再作為入口使用）

### 3. 後台頁面實測

#### 建立頁 `admin.html`
- 頁面正常開啟
- 頁面頂部只剩「查詢保固書」按鈕，沒有舊的「後台首頁」按鈕
- console 無錯誤

#### 查詢頁 `query.html`
- 可從建立頁直接進入
- 頁面正常顯示案件分組
- 實測畫面內容包含：
  - 保固中案件（1）
  - 案件編號 `FG-2026-003`
  - `LINE 已綁定`
  - `保固生效中`
- console 無錯誤
- 可返回建立頁

#### 入口頁 `portal.html`
- 標題已更新為 `楓根後台入口`
- live 畫面顯示：
  - `建立保固書`
  - `查詢保固書`
  - `切換 Google 帳號`
- 實點 `建立保固書` 後，可正常進入 `電子保固書建立`

### 4. 客戶頁檢查

#### 保固頁 `index.html`
- source 與 API 設計已切到 token 模式
- `id=` 舊參數路線在程式中已明確標示為舊連結，不再作為正式公開查詢入口
- `token=` 路線對應 `getWarrantyByToken`

#### 報修頁 `repair.html`
- source 顯示需帶 `token`
- 若缺 token，頁面會顯示：`缺少保固 token，請從新版保固書連結重新開啟報修頁。`
- 報修送出邏輯仍存在，但本次未做實際寫入測試，避免產生真實報修資料

## 四、本次未做的動作

以下為避免污染正式資料，本次**刻意未執行**：
- 建立新保固書
- 實際送出報修單
- 修改既有案件
- 刪除案件
- 核銷操作

也就是說：
- **讀取/導覽/查詢路線已驗證**
- **寫入型流程尚未做正式 smoke test**

## 五、建議下一步

### 若要補完整驗收
1. 做一次「低風險正式 smoke test」
   - 建立 1 筆測試案件
   - 取得 token 保固頁
   - 送 1 筆測試報修
   - 到查詢頁確認案件與報修都能看到
   - 最後刪除測試資料或標示為測試案

### 若要再往安全面收斂
2. 繼續完成你剛才定的方向
   - `admin.html / query.html` 退出公開站
   - 前端移除 `adminSendPin`
   - Apps Script 管理 API 加保護
   - 客戶頁全面 token 化

## 六、結論

目前公開站入口已整理到一致：
- `portal.html` 可用
- `admin.html` 可用
- `query.html` 可用
- 客戶頁 token 讀取 API 可用

也就是說，**先前唯一明確壞掉的 `portal.html` 入口已修復完成**。

目前剩下的重點不是入口壞掉，而是：
- 寫入型流程還沒做正式 smoke test
- 以及是否要繼續把後台完全退出公開站、把管理 API 再往上鎖

---

> 備註：本紀錄刻意不寫入管理金鑰與 token 明碼；敏感值均以 live 驗證結果為準，不在文件中保存。