# 電子保固卡進度紀錄

- 更新時間：2026-07-10
- 專案位置：`E:/電子保固書`
- 公開網址：`https://harry0715-glitc.github.io/electronic-warranty-book/`
- LIFF 入口：`https://liff.line.me/2010316548-KZgCnSKp`
- 管理後台（Google 登入，加入書籤）：
  - 建立：`https://script.google.com/macros/s/AKfycbxK3U_BbLdI335S6vUn_RX93_CzNARuj4upTDVkwySJBtfpNVqNnriKQZm91qJrf1QsIw/exec?page=admin`
  - 查詢：同網址 `?page=query`

---

## ✅ 已完成

### 基礎功能（6 月前已完成）
- 保固書建立 / 查詢 / token 化顯示 / 報修流程全通
- LINE 手機綁定、管理端推送保固資訊卡、報修建立與完工通知
- clasp 本機綁定（harry0715@gmail.com）

### 2026-07-10 安全修正（P0 全數完成，程式碼層）
1. 移除 Code.gs 硬編碼 PIN `521527` 與 fallback 鏈，金鑰只認 Script Properties `ADMIN_API_KEY`
2. `docs/` 移除 `admin.html`、`query.html`；`portal.html` 改指向部署 B
3. 管理 API 由 GET+JSONP 改為 POST，adminKey 移入 body；Code.gs 新增 `routeAdminAction_` 統一路由
4. LINE webhook：key 未設定或不符一律拒絕
5. `createRepair`：公開請求必須帶有效 token + CacheService 頻率限制

### 2026-07-10 部署 A 更新到新版本（完成）
- 管理部署作業「Rollback admin deployment」更新為 51 版（含全部 P0 安全修正 + Google 登入後台）
- 部署後驗證：客戶保固頁 `?token=...` 正常載入即時資料，API 正常運作

### 2026-07-10 金鑰輪換（完成）
- Script Properties 三組新隨機值已存檔：`ADMIN_API_KEY`、`LINE_ADMIN_SEND_PIN`、`LINE_WEBHOOK_KEY`（舊 PIN 521527 已停用）
- LINE Developers webhook URL 已同步改用新 `key` 參數（Han 自行更新確認完成）
- 新金鑰僅存於 Script Properties 與 LINE Developers 後台，未寫入 repo

### 2026-07-10 Google 登入後台（完成並實測可用）
- 建立部署 B（執行身分：存取的使用者 / 任何 Google 帳號），50 版
- **根因排除**：manifest `oauthScopes` 缺 `"openid"` 導致 `getActiveUser().getEmail()` 永遠回空值——已補上，登入後正常進入後台
- AdminUsers 工作表已自動建立 owner 列（harry0715@gmail.com / manager / TRUE）
- manifest webapp 已還原匿名預設（保護公開 API 部署不被改壞），本機與線上一致

### 2026-07-10 公開入口 / 備援頁整理（本機修改已完成，待 Git 收尾）
- `docs/admin.html`、`docs/query.html` 已移除，公開站不再直接暴露管理頁
- `docs/portal.html` 已改成單一入口頁，按鈕導向部署 B 的 `?page=admin` / `?page=query`
- 專案根目錄仍保留 `admin.html`、`query.html` 作為本機備援管理頁（走 POST + `ADMIN_API_KEY`）
- 最近提交重點：公開入口導流、query 備援按鈕修正、public admin/query pages API target 修正

## ⬜ 未完成 / 待辦（依優先序）

1. **Git 收尾**
   - `git add -A` → `git commit` → `git push`
   - GitHub Pages source 切到 `main /docs`（切完後公開站才真正沒有管理頁）
2. **寫入流程 smoke test**：建測試案件 → token 開保固頁 → 送測試報修 → 查詢確認 → 刪除測試資料
3. **部署清理**：封存無用部署——「部屬B」（AKfycbxmhEn1...）、未命名（AKfycbxvF8e...）、Warranty DB API test deploy、舊未命名（AKfycbxhiL2...，確認 LINE webhook 不指向它後再封存）
4. **P1 優化**（見架構掃描報告）：root/docs 去重、字型子集化（30MB→數百KB）、Code.gs 拆分模組、README 占位電話補真實值
5. **P2**：Sheet 每日自動備份、保固到期提醒（價值最高的擴充，建議最先做）

## 已知注意事項

- 金鑰已輪換完成；待辦 1（Git 收尾）盡快做完，避免舊 repo 歷史中的舊 PIN 疑慮拖太久
- `appsscript.json` 的 `"openid"` scope 不可移除
- 新增管理類部署時，部署對話框要手動選「存取網頁應用程式的使用者」
- 未來重新部署部署 B 後若登入異常，先檢查 manifest webapp 設定是否被套成匿名

## 歷程備註

- 6/22：webhook 綁定提示誤觸發已修（`findLineContactByUserId_`）；系統檢測見 `電子保固書系統檢測紀錄_2026-06-22.md`
- 7/10：完整架構掃描與 P0 安全修正，見 `架構掃描與優化建議_2026-07-10.md`；Google 登入後台排錯過程見 `後台Google登入設定指南.md`
- 7/10 下午：部署 A 更新為 51 版並驗證公開頁正常，P0 安全修正正式上線
- 7/10 下午：金鑰輪換完成，ADMIN_API_KEY / LINE_ADMIN_SEND_PIN / LINE_WEBHOOK_KEY 全數換新，LINE webhook URL 同步更新
